import initRequired from "./init-required.md" with { type: "text" };
import workflowTemplate from "./workflow.md" with { type: "text" };

export const MCP_WORKFLOW_TEMPLATE = workflowTemplate.trim();
export const MCP_INIT_REQUIRED_GUIDE = initRequired.trim();
